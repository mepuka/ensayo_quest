[test]
# Exclude Effect source (symlinked from ~/.local/share/effect-solutions/effect)
# The symlink is followed by bun, so we exclude both the link and target paths
exclude = [
  "effect/**",
  "**/effect/packages/**",
  "effect-atom/**",
  "**/effect-atom/packages/**",
  "node_modules/**"
]
